import React from 'react'
import Modal from '../../../Components/Modal'

const OrderDetailsView = ({open, setOpen}) => {
  return (
    <Modal open={open} setOpen={setOpen}>

    </Modal>
  )
}

export default OrderDetailsView